# Describe the commands of servers you want to start, one per line.
# Example: /usr/bin/python3 /path/to/my_server.py --port 8080
#
# Optional: Specify custom ID for a server by adding id= before the command
# Example: id=my-server /usr/bin/python3 /path/to/my_server.py --port 8080
#
# After editing this file, copy it to /etc/mcp/mcp_server.conf and
# restart the service to reload the configuration with the following command:
# sudo systemctl restart mcp-manager.service

# Sample configurations (uncomment to use):
# id=web-server /usr/bin/python3 -m http.server 8080
# id=api-server node /path/to/api/server.js --port 3000
# /usr/bin/python3 /path/to/worker.py  # Auto-generated hash ID
# id=database redis-server /etc/redis/redis.conf
