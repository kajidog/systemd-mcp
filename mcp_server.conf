# 起動したいサーバーのコマンドを1行に1つずつ記述します。
# 例: /usr/bin/python3 /path/to/my_server.py --port 8080
#
# このファイルを編集した後は、/etc/mcp/mcp_server.conf にコピーし、
# 以下のコマンドでサービスを再起動して設定を再読み込みしてください:
# sudo systemctl restart mcp-manager.service
